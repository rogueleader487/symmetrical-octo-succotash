namespace Kx.Availability.Data.Common;

/*
 * This feels like something that should belong in a Core project for reuse - it doesn't belong in Implementation and potentially
 * outside of Availability.Data
*/
public enum LocationType
{
    Locations,
    Rooms
}
